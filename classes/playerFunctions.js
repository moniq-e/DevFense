import { Board } from "./Board.js"
export class PlayerFunctions {
    stage
    /**
     * @param {Board} board 
     */
    constructor(board) {
        this.stage = 0
    }
    // teste
    static consoleLog(text) {
        console.log(text)
    }
}